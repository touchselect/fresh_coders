.container
  .form
    h1 投稿の編集
    = form_with(model: @post, local: true) do |f|
      h4 サムネイル画像
      = f.file_field :post_image, accept: "image/*"
      h4 タイトル
      = f.text_field :title
      h4 コンテンツ
      .field
        = f.rich_text_area :content
      = f.submit "変更を保存する", class: "btn btn-primary"
      = f.button "非公開にして下書きに保存", name: "draft_button", value: "true", class: "btn btn-secondary"