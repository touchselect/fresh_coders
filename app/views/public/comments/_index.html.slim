.comments
  - post.comments.each do |comment|
    .comment
      p = comment.user.name
      p = comment.created_at.strftime('%Y/%m/%d')
      p = comment.comment
      .comment-btn
        - if comment.user == current_user
          = link_to "削除", post_comment_path(post, comment), method: :delete, class: 'btn btn-danger', local: false, remote: true
